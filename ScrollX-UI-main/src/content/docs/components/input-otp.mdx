---
title: Input Otp
description:  A flexible Accessible one-time password component
---

<ComponentPreview
  name="inputotp-demo"
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  
  <TabsContent value="cli">
    <PkgOptions name="input-otp" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>input-otp.tsx</code>
</p>
<ComponentSource name="input-otp" />

<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx
 import {(ClassValue, clsx)} from "clsx";
  import {twMerge} from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>


## Usage
```tsx
import {
  InputOTP,
  InputOTPGroup,
  InputOTPSeparator,
  InputOTPSlot,
} from "@/components/ui/input-otp"
```

```tsx
<InputOTP maxLength={6} mask={true} maskDelay={1200}>
  <InputOTPGroup>
    <InputOTPSlot index={0} />
    <InputOTPSlot index={1} />
    <InputOTPSlot index={2} />
  </InputOTPGroup>
  <InputOTPSeparator separatorSymbol="+"  />
  <InputOTPGroup>
    <InputOTPSlot index={3} />
    <InputOTPSlot index={4} />
    <InputOTPSlot index={5} />
  </InputOTPGroup>
</InputOTP>
```


## API Reference

 ### Input Otp

The `InputOTP` is a root component that creates an animated OTP input field with masking support and paste functionality.

## Props

<PropsTable
  rows={[
    {
      prop: "maxLength",
      type: "number",
      default: "required",
      description: "Total number of OTP digits. Used in: InputOTP."
    },
    {
      prop: "onComplete",
      type: "(otp: string) => void",
      default: "—",
      description: "Callback fired when OTP is fully entered. Used in: InputOTP."
    },
    {
      prop: "mask",
      type: "boolean",
      default: "false",
      description: "Whether to mask each digit. Used in: InputOTP."
    },
    {
      prop: "maskSymbol",
      type: "string",
      default: "•",
      description: "Symbol to show when masking. Used in: InputOTP."
    },
    {
      prop: "maskDelay",
      type: "number",
      default: "1000",
      description: "Delay (ms) before masking input. Used in: InputOTP."
    },
    {
      prop: "index",
      type: "number",
      default: "required",
      description: "Slot index position. Used in: InputOTPSlot."
    },
    {
      prop: "separatorSymbol",
      type: "string",
      default: "'-'",
      description: "Symbol displayed between OTP slots. Used in: InputOTPSeparator."
    },
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Optional styling class. Used in: InputOTP, InputOTPGroup, InputOTPSlot, InputOTPSeparator."
    },
    {
      prop: "containerClassName",
      type: "string",
      default: "—",
      description: "Class name for outer container. Used in: InputOTP."
    },
    {
      prop: "inputClassName",
      type: "string",
      default: "—",
      description: "Class name for each OTP input. Used in: InputOTP."
    },
    {
      prop: "children",
      type: "ReactNode",
      default: "optional",
      description: "Nested content. Used in: InputOTP, InputOTPGroup."
    }
  ]}
/>

