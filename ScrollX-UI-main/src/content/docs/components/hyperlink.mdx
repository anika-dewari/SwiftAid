---
title: Hyperlink
description: Hyperlink component with animated underline, smooth hover & customizable style.
---

<ComponentPreview
  name="hyperlink-demo"
  className=""
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="hyperlink" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>


<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>hyperlink.tsx</code>
</p>
<ComponentSource name="hyperlink" />

<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```


</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { Hyperlink } from "@/components/ui/hyperlink";
```

```tsx
<Hyperlink href="/components" className="text-lg">
  Components
</Hyperlink>
```
## Examples

---

<Step>Custom</Step>

<ComponentPreview
  name="hyperlinkcustom-demo"
  className=""
  description=""
/>

## API Reference

### Hyperlink

Hyperlink component with animated underline, smooth hover & customizable style.

## Props

<PropsTable
  rows={[
    {
      prop: "href",
      type: "string",
      default: "required",
      description: "URL the link navigates to when clicked."
    },
    {
      prop: "children",
      type: "React.ReactNode",
      default: "required",
      description: "Content displayed inside the hyperlink, e.g., text, button, or icon."
    },
    {
      prop: "className",
      type: "string",
      default: "optional",
      description: "Custom CSS classes applied to the link container for styling."
    },
    {
      prop: "underlineClassName",
      type: "string",
      default: "optional",
      description: "Custom CSS classes applied to the animated underline."
    }
  ]}
/>

