---
title: Alert Dialog
description: A modal dialog used to convey critical information and require a user response before proceeding..
---

<ComponentPreview
  name="alertdialog-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="An accessible Alert Dialog component built with Radix UI"
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="alert-dialog" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="@radix-ui/react-alert-dialog framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>alert-dialog.tsx</code>
</p>
<ComponentSource name="alert-dialog" />


<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog";
```

```tsx
<AlertDialog>
  <AlertDialogTrigger>Open</AlertDialogTrigger>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>Are you absolutely sure?</AlertDialogTitle>
      <AlertDialogDescription>
        This action cannot be undone. This will permanently delete your account
        and remove your data from our servers.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Cancel</AlertDialogCancel>
      <AlertDialogAction>Continue</AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

## API Reference

### AlertDialog

The root container for the alert dialog component.

## Props

<PropsTable
  rows={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description: "Render the trigger as its child element. Used in: AlertDialogTrigger."
    },
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Optional class name for custom styling. Used in: Content, Header, Footer, Title, Description, Action, Cancel."
    }
  ]}
/>



