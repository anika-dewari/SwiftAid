import { Loader } from "@/components/ui/loader";

export default function LoaderMagneticdots() {
  return (
    <Loader variant="magnetic-dots">
      <span className="text-black dark:text-white">Hang tight</span>
    </Loader>
  );
}
